# Single source of truth for dependencies is pyproject.toml.
# This file installs the package (and its runtime deps) in editable mode so
# the documented `pip install -r scripts/requirements.txt` / uv paths keep working.
-e .

# Optional PDF rendering (needs system libs — see README):
#   pip install -e ".[pdf]"
# MCP server:
#   pip install -e ".[mcp]"
# Contributor tooling:
#   pip install -e ".[dev]"
